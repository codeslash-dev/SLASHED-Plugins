# Files under src/ that sync-core.mjs must NOT overwrite.
#
# Plugin-specific build wiring lives OUTSIDE src/ (vite.config.js, package.json,
# svelte.config.js, tsconfig.json) and is never touched by the sync.

# src/main.ts is listed here because the upstream @framework-css alias and the
# #slashed-admin-app mount-target change live on a feature branch that has not
# yet merged to main. Once codeslash-dev/slashed#443 merges, remove this line
# so future upstream changes to main.ts flow through automatically.
src/main.ts
